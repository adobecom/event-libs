// Defined once in v1/utils/tier-1-event-config.js, re-exported here so this app doesn't
// carry a second, independently-drifting copy.
export { DEFAULT_ICON_COLOR } from '../v1/utils/tier-1-event-config.js';
