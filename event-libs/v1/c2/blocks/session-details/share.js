/*
 * Share (MWPW-203472) — sub-feature of session-details.
 * Icon buttons / copy-link / native share. Persistent across all states.
 * Reads top-level `url` + `title`.
 */

export function renderShare(/* { url, title } */) {
  // TODO(MWPW-203472)
  return null;
}
